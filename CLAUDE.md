# הנחיות לסוכן שעובד על הריפו הזה

קרא את זה במלואו לפני שאתה נוגע במשהו. הוא כתוב עבור סוכן שמגיע למכונה חדשה
ולא היה כאן קודם.

---

## מה הריפו הזה

מערך שיווק לכל מוצר של קבוצת **TACT**. תיקייה אחת למותג; `bedek/` הוא הראשון
והוא גם התבנית. איך משווקים מוצר חדש — ב-[PLAYBOOK.md](PLAYBOOK.md).

## הכלל היחיד שאסור להפר

```
git          מה שמייצר   סקריפטים, קוד, החלטות, טקסטים
SharePoint   מה שנוצר    וידאו, PDF, תמונות   (הארגון צופה מכאן)
S3/CloudFront מה שמוגש   רק מה שצריך URL ציבורי
```

**לריפו נכנס רק מה שמייצר.** אם אתה עומד להוסיף `.mp4`, `.png` שנוצר בסקריפט,
או תיקיית `out/` — עצור. הסקריפט שמייצר אותם הוא הנכס, לא הפלט.

`.gitignore` אוכף את זה. אל תעקוף אותו ב-`git add -f`.

## סודות

**אין ולא יהיה סוד בריפו הזה.** כל המפתחות יושבים בקובץ משותף מחוץ לריפו:

```
C:\Users\User\Aiprojects\env\.env          ברירת מחדל
TACT_ENV=/path/to/.env                     דריסה במכונה אחרת
```

מפתחות שהסקריפטים כאן קוראים:

| מפתח | למה |
|---|---|
| `FALAI_API_KEY` | יצירת קליפי וידאו |
| `ELEVENLABS_API_KEY` | קריינות עברית |
| `CLOUDFLARE_API_TOKEN` | DNS — מוגבל ל-Zone:DNS:Edit על `yazam-il.com` בלבד |
| `SHAREPOINT_TENANT_ID` · `_CLIENT_ID` · `_CLIENT_SECRET` · `_SITE_URL` | העלאת תוצרים |
| `TELEGRAM_BOT_TOKEN` | התראות לידים |

⚠️ **ל-`.env` המשותף אין שורה חדשה בסוף.** `grep '^KEY=...'` מפספס את השורה
האחרונה. כל הסקריפטים כאן מנתחים את הקובץ ולא מתאימים שורה — אל תחזור לגרפ.

## דרישות מקדימות

```bash
python 3.13+          # ספרייה תקנית בלבד, אין requirements.txt
pip install playwright pillow numpy && playwright install chromium
node 22+              # לאתר בלבד
ffmpeg + ffprobe      # בנייה של וידאו
aws cli               # פריסה
gh cli                # אופציונלי
```

⚠️ **ב-Windows `aws` הוא `aws.cmd`** — `subprocess` לא מוצא `aws` חשוף.
הסקריפטים משתמשים ב-`shutil.which`. אל תסיר.

---

## מבנה המותג

```
bedek/
  PLAN.md            תוכנית העבודה וסטטוס כל שלב
  DECISIONS.md       כל החלטה + הנימוק + מה נדחה ולמה
  brand/             פלטה ולוגו — סקריפטים, לא קבצים
  content/           messaging.md + מחולל ה-OnePage
  site/              אתר React+Vite
  social/            פייסבוק/אינסטגרם
  video/             סרטונים
  ops/               דומיין, פריסה, לידים, SharePoint
```

**`DECISIONS.md` הוא המסמך החשוב ביותר.** לפני שאתה משנה כיוון, קרא אותו —
רוב מה שנראה כמו החלטה שרירותית כבר נשקל ונדחה מסיבה שכתובה שם.

## איך מייצרים הכל מאפס

הסדר חשוב — כל שלב צורך את קודמו:

```bash
cd bedek

# 1. זהות: פלטה -> טוקנים, לוגו -> SVG
python brand/palette/palette.py      # OKLCH -> directions.json + tokens.css
python brand/palette/contrast.py     # שער WCAG — יוצא עם 1 על כל כשל
python brand/logo/kit.py             # הסימן -> assets/logos/

# 2. אתר
cd site && npm install
python sync-brand.py                 # מושך פלטה, פונטים, לוגו, 404, פרטיות
npm run dev                          # פורט 5173

# 3. סרטונים  (⚠️ עולה כסף — קרא video/README.md קודם)
cd ../video
python make_vo.py                    # קריינות ראשונה! אורכה קובע את הקצב
python make_clips.py                 # ~0.10$ לקליפ
python make_cards.py
python make_music.py
python build.py

# 4. מסמכים ותוצרים
python ../ops/md2pdf.py              # מרקדאון -> PDF ממותג
python ../ops/sharepoint.py --plan   # תמיד --plan קודם
python ../ops/sharepoint.py
```

## פריסה

```bash
cd bedek/ops
python deploy.py publish             # בנייה -> S3 -> פינוי מטמון
python cf.py list                    # רשומות DNS
python cf.py ensure CNAME app <target> false
```

⚠️ **תמיד `false` לפרוקסי** ברשומות שמצביעות ל-CloudFront. ענן כתום מוסיף
CDN שני מעל CDN, ופינוי מטמון ב-AWS מפסיק להשפיע על מה שהגולש רואה.

---

## כללים שאסור להפר בתוכן

שני דברים אסורים בכל טקסט שיוצא ללקוח:

1. **לעולם לא "עומד בדרישות חוק המכר"** — זו הבטחה משפטית. הניסוח המותר:
   *"בנוי לתיעוד הנדרש בתקופת הבדק"*.
2. **לעולם לא Google Play** — אין גרסת אנדרואיד. הניסוח: *"אפליקציה לאייפון;
   אנדרואיד בקרוב"*.

**כל טקסט באתר יושב ב-`site/src/content/copy.ts` בלבד.** רכיב לא מכיל משפט.
מקור האמת הרעיוני הוא `content/messaging.md`.

---

## מלכודות שכבר שילמנו עליהן

אל תלמד אותן שוב.

### עברית ב-TTS
**נקד כל מילה דו-משמעית**, לא רק שם המותג. `בדק` בלי ניקוד נהגה *vedek*;
`מרכז` נקרא כשם עצם במקום כפועל. `eleven_v3` הוא **המודל היחיד** שמדבר עברית.
לאימות הגייה — תמלל בחזרה עם `fal-ai/speech-to-text`; למפתח ElevenLabs **אין**
הרשאת `speech_to_text`.

### וידאו
- **`fal-ai/wan/v2.2-a14b/text-to-video/turbo` מחייב 0.10$ ל-CLIP, לא לשנייה.**
  זו ההבחנה שקובעת תקציב. התקציב בפועל הוא ~15$, לא מאות.
- **המילה `photographing` בפרומפט מכניסה מצלמת קולנוע לפריים.** תאר פעולה,
  לעולם לא ציוד.
- **כל cross-dissolve מקצר את התמונה.** תוזמון הקריינות חייב לרוץ על הציר
  החופף, אחרת `-shortest` **חותך את המשפט האחרון בשקט**, בלי שגיאה.
- **פלט חייב 30fps ו-keyframe כל שנייתיים.** 32fps עם 3 keyframes מתנגן
  2-3 שניות ונתקע בנגנים בסיסיים, למרות שהקובץ מפוענח מושלם ב-ffmpeg.
- **AI לא מרנדר עברית קריאה.** כל טקסט שהצופה קורא — Chromium, לא מודל.
- **`@keyframes` אינו דטרמיניסטי** ברנדור פריים-פריים. חושפים `SEEK(t)` טהורה.

### CloudFront
- **תבניות נתיב יחסיות:** `api/lead`, לא `/api/lead`. עם לוכסן מוביל התבנית
  לא מתאימה לכלום והבקשה נופלת ל-S3.
- **הקידומת `X-Edge-*` שמורה** ונדחית ב-`CustomHeaders`.
- **`UpdateDistribution` דורש את מלוא שדות ה-legacy** ומדווח על אחד חסר בכל
  סבב. הסקריפטים מציינים את כולם מראש.
- **מיפוי 403→404.html מסווה שגיאות origin.** כשמשהו לא מסתדר — כבה זמנית
  את `CustomErrorResponses` כדי לראות סטטוס אמיתי.

### לידים
**אסור `CRM_API_KEY` במשתנה `VITE_*`** — הוא נצרב ל-bundle ומתפרסם. הטופס
שולח ל-`/api/lead`; פרוקסי מחזיק את המפתח. שתי ארכיטקטורות נכשלו לפני זו,
מתועדות ב-`bedek/ops/leads.md` — אל תחזור אליהן.

### DNS
**רשומות TXT מזוהות לפי המפתח שלהן** (`v=spf1` מול `facebook-domain-verification`),
לא לפי שם+סוג. `cf.py` תוקן אחרי שהחליף SPF חי באימות פייסבוק.

### מטא
**טוקן נצחי דורש ארבעה קשרים נפרדים** ולכל אחד שגיאה מטעה. הקריטי:
**System User ↔ דף** הוא שיוך **נפרד** מ-System User ↔ אפליקציה; בלעדיו
הטוקן תקין לגמרי אבל `me/accounts` מחזיר ריק. מלא ב-`bedek/social/meta-setup.md`.

### SharePoint
מרקדאון מוצג כטקסט גולמי — הרץ `ops/md2pdf.py` קודם. קבצים מעל 4MB דורשים
`createUploadSession` בנתחים, לא `/content`.

---

## עקרונות עבודה

**כל דבר שנוצר, נוצר בסקריפט.** לא פלטה שהוקלדה ביד, לא לוגו שיוצא מכלי
עיצוב, לא PDF שהורכב ידנית. אם אי אפשר לייצר מחדש בפקודה — זה באג.

**מפרט מיוצר דורש בדיקה מיוצרת.** `contrast.py` יוצא עם 1 על כל צמד שנכשל
ב-WCAG. בדיקה ידנית פעם אחת היא כשל שעובר בירושה לכל הכיוונים הבאים.

**בדוק את הפלט, לא את הלוג.** צלם את העמוד, פענח את הווידאו, קרא בחזרה
מ-SharePoint, בדוק את ה-DNS מול שרת חיצוני. "הפקודה הצליחה" הוא לא אימות.

**כתוב את הסיבה, לא רק את הכלל.** כל אזהרה כאן עלתה זמן אמיתי. הערה שמסבירה
*למה* מונעת את החזרה; הערה שרק אוסרת מוזמנת להימחק.
